const SubHeading = ({subheading}) => {
    return (             
        <div>
            <h2 className="my-5 after:text-center text-sm text-black">{subheading}</h2>
        </div> 
    );
}
 
export default SubHeading;